# ── Secrets — NEVER commit (live API keys live here) ──
.env
*.env
.env.*

# ── Python ──
backend/.venv/
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# ── Databases / local state ──
*.db
*.sqlite
*.sqlite3
backend/dsource.db

# ── Large binary assets (fetch locally; see docs/data-sourcing.md) ──
backend/data/pricebooks/*.pdf
*.glb
*.gltf

# ── Steelcase application DWGs + built settings library (licensing; not redistributable) ──
data/steelcase/

# ── Node / frontend ──
node_modules/
frontend/node_modules/
frontend/dist/
frontend/.vite/
*.log

# ── OS / editor ──
.DS_Store
.idea/
.vscode/
*.swp

.claude/sdd-cache/
.playwright-mcp/

# Steelcase application library (DWGs + built settings.json) — licensing, not committed
steelcase/
steelcase-dxf/
frontend/node_modules
